import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Customer",
};

export default function Customers() {
  return (
    <div>
      <p>Customers Page</p>
    </div>
  );
}
